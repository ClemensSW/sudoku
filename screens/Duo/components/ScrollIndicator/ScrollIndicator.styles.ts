// screens/DuoScreen/components/ScrollIndicator/ScrollIndicator.styles.ts
import { StyleSheet } from "react-native";

export default StyleSheet.create({
  scrollIndicator: {
    marginTop: 30,
    marginBottom: 10,
    alignItems: "center",
    padding: 12,
  },
  scrollButton: {
    alignItems: "center",
  },
  scrollText: {
    // fontSize set dynamically via theme.typography
    fontWeight: "600",
    marginBottom: 8,
  },
});