rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // ===== Temporary Rules (Phase 1) =====
    // Allow all authenticated users (wird in Phase 1.2 ersetzt)

    match /{document=**} {
      allow read, write: if request.auth != null;
    }

    // ===== Final Rules (TODO: Implement in Phase 1.2) =====
    // Siehe PLAN.md Section 5 für vollständige Security Rules
  }
}
